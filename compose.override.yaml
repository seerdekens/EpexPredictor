services:
  epexpredictor:
    build: .
    ports:
      - "8020:80"
    volumes:
      - ${DATA_PATH}:/data
    environment:
      - EPEXPREDICTOR_DATADIR=/data/
      - EPEXPREDICTOR_ENTSOE_API_KEY=${MY_ENTSOE_API_KEY}
